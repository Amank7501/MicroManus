# MicroManus — environment variables
# Copy to .env.local and fill in values. Never commit .env.local.

# --- Phase 1: Supabase (auth, DB, key storage) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY= (not needed until we write to the DB directly from the server, phase 2+)

# --- Phase 3: encryption for user-supplied LLM API keys ---
# API_KEY_ENCRYPTION_SECRET=

# --- Phase 5: web search ---
# TAVILY_API_KEY=

# --- Phase 8: payments ---
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
